import { Button, StyleSheet, Text, View } from 'react-native'
import React from 'react'

export default function LocationManager() {
  return (
    <View>
      <Button title="Fine my location" onPress={() => {}} />
    </View>
  )
}

const styles = StyleSheet.create({})