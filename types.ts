export type Regio = {
  latitude: Number,
  longitude: Number,
  latitudeDelta: Number,
  longitudeDelta: Number,
}

export type LocationData = {
  longitude: number,
  latitude: number,
}

export type LatLng = {
  latitude: number,    
  longitude: number, 
}